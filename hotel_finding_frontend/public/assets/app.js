document.addEventListener('DOMContentLoaded', () => {
  // Global app initializer
  console.log('App initialized. Screen loaded:', document.body.querySelector('.screen-container')?.id || 'unknown');

  // No interactive behavior required for Open Page screen.
});
